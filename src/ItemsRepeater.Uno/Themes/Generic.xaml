<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Avalonia.Controls"
    xmlns:data="using:Avalonia.Controls.DataGrid"
    xmlns:layout="using:Avalonia.Layout">
    <Style TargetType="data:RepeaterDataGrid">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="data:RepeaterDataGrid">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border x:Name="PART_HeaderHost"
                                Grid.Row="0"
                                BorderBrush="#FFD9D9D9"
                                BorderThickness="1,1,1,0">
                            <controls:ItemsRepeater x:Name="PART_HeaderRepeater"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Top"
                                                    Height="{Binding HeaderHeight, RelativeSource={RelativeSource TemplatedParent}}">
                                <controls:ItemsRepeater.Layout>
                                    <layout:StackLayout Orientation="Horizontal"
                                                        DisableVirtualization="True" />
                                </controls:ItemsRepeater.Layout>
                            </controls:ItemsRepeater>
                        </Border>

                        <ScrollViewer x:Name="PART_BodyScrollViewer"
                                      Grid.Row="1"
                                      HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollMode="Enabled"
                                      VerticalScrollMode="Enabled"
                                      IsScrollInertiaEnabled="True"
                                      IsHorizontalRailEnabled="True"
                                      IsVerticalRailEnabled="True"
                                      ZoomMode="Disabled">
                            <controls:SelectingItemsRepeater x:Name="PART_RowsRepeater"
                                                             Background="Transparent"
                                                             HorizontalAlignment="Left"
                                                             VerticalAlignment="Top">
                                <controls:SelectingItemsRepeater.Layout>
                                    <layout:StackLayout Orientation="Vertical" />
                                </controls:SelectingItemsRepeater.Layout>
                            </controls:SelectingItemsRepeater>
                        </ScrollViewer>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
